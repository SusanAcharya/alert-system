// This script requires TypeScript compilation first
// For now, use the API endpoint or compile TypeScript
// Run: npx tsx scripts/start-scheduler.ts
// Or use the API: POST /api/scheduler/start

console.log('Please use one of these methods to start the scheduler:');
console.log('1. Call POST /api/scheduler/start endpoint');
console.log('2. Install tsx: npm install -g tsx');
console.log('3. Run: npx tsx scripts/start-scheduler.ts');

